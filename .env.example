# Public frontend config (safe to expose in the client bundle).
# For GitHub Pages, set this to your deployed API origin, for example:
# VITE_CHAT_API_BASE_URL=https://portfolio-ht.vercel.app
VITE_CHAT_API_BASE_URL=

# Do NOT add OPENAI_API_KEY here.
# Keep it server-only in GitHub Secrets and Vercel runtime env.
